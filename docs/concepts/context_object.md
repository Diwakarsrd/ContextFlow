# Context Object

The atomic unit every connector, retriever, and compiler in ContextFlow
speaks. See `src/contextflow/core/context.py` for the full schema and
`ARCHITECTURE.md` for how it fits into the data flow.
